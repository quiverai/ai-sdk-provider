/**
 * QuiverAI image model identifier.
 */
export type QuiverAIImageModelId = "arrow-preview" | (string & {});

/**
 * Aspect ratio with the format `{width}:{height}`.
 */
export type QuiverAIAspectRatio = `${number}:${number}`;
